import './Projects.css';
import React, {useState} from "react";

export default function ProjectsHome() {
  return (
    <div className="Page">
        <section className = "Title">
          Projects
        </section>
        <section className = "Text">
          This is a placeholder -- when finished, it will hold the various projects I am working on!
        </section>
    </div>
  );
}